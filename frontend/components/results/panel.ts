/** Shared panel chrome for the Results/Story tabs — one definition so the
 * two halves of the run screen can never drift apart. */
export const PANEL = "rounded-[14px] border border-line bg-panel";
export const PANEL_TITLE = "font-mono text-[11.5px] font-medium tracking-[.12em] text-ink-4";
