# Skeptic landing — design export (for L4)

Source of truth: `Skeptic Landing.dc.html` at the project root — option **2a** (desktop, scrollable frame) and **2b** (mobile, scrollable frame). Turn 1 options are history; build from turn 2.

## Files here
- `copy-deck.md` — every string on the page, desktop + mobile, with provenance flags
- `component-specs.md` — build specs per component (hero embed, wordmark morph, verdict showcase, receipts, pricing, copilot demo, footer)
- `asset-requests.md` — recording clips to export (timestamps, crops, loops), stills, brand files
- `stills/` — cropped poster frames from the recordings (light-theme captures; see theme decision)

## Page order (2a)
hero → how it argues → the verdict (refusal leads + range) → receipts → coming next (copilot) → pricing → footer

## Open decisions (owner)
1. **Clip theme** — recordings are light (market-hours). Re-record dark (Settings → theme) or keep paper-on-ink stills.
2. **Pinned run ids** — refused run (low-trade-count cause), blessed/graded run, and the real "84% lucky days" SPY .30Δ short put (REC 1 @ 1:05) for the range rail.
3. **Disclaimer** — removed from landing body/footer per owner (Jul 16). PRD H marks it load-bearing on landing + checkout + results; resolve with legal-pages pass.
4. **"since 1993"** (chart-teach line, on page in S2) vs window picker's 3,480 sessions (~14y) — confirm wording survives fact-check.
5. **"2009 chains" tile** — hand-verify from the coverage endpoint before launch.

## Non-negotiables carried from the brief
No P/L red-green on trust/verdict surfaces (candles + P/L numbers only). No fake counters, countdowns, testimonials, up-only equity decoration. Every number real or flagged above.
