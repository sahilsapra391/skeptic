# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / Next.js
node_modules/
.next/
*.tsbuildinfo

# Secrets / env — keys never go in git
.env
.env.*

# Market data never goes in git — R2 only (standing rule).
# Root-anchored: app route folders like frontend/app/data/ are code, not data.
/data/
*.parquet

# Local tooling
.claude/settings.local.json

# Raw handoff copy (canonical versions live in docs/ and collector/)
/files/

# but the template documenting the local env file is not a secret
!.env.example
